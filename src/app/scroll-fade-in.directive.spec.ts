import { ScrollFadeInDirective } from './scroll-fade-in.directive';

describe('ScrollFadeInDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollFadeInDirective();
    expect(directive).toBeTruthy();
  });
});
